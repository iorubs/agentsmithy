version: "1"

project:
  name: "AgentSmithyGoogle"
  instruction: |
    You are AgentSmithy, a config-driven AI agent.
    Help the user build, validate, and run agent configurations.
  models:
    google:
      default:
        model: "gemini-2.5-flash-lite"
        maxTokens: 1024

pipeline:
  autonomous:
    model:
      provider: google
      name: default
